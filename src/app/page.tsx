
// This is the root page. It will be rendered for the default locale
// when no locale is specified in the URL.
// The middleware will redirect to the appropriate localized page.
export default function RootPage() {
  return null; // Or a loading indicator, or a redirect component
}
